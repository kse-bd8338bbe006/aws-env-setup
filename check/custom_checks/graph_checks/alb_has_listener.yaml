metadata:
  id: "CUSTOM_GRAPH_1"
  name: "Ensure ALB has at least one listener configured"
  category: "NETWORKING"

definition:
  and:
    - cond_type: "connection"
      resource_types:
        - "aws_lb"
      connected_resource_types:
        - "aws_lb_listener"
      operator: "exists"
